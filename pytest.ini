[pytest]
norecursedirs=
    *.egg
    .git
    .tox
    .env
    _sass
    build
    dist
    migrations
python_files =
    test_*.py
    tests.py

addopts=
   --cov=transliterate
   --ignore=.tox
   --ignore=requirements
   --ignore=release
